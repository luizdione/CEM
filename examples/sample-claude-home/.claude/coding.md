# Coding standards

- Prefer strict TypeScript configuration in every project.
- Prefer composition over inheritance for shared behaviour.
- Use descriptive names; avoid abbreviations.
- Small, single-purpose functions and modules.
- Document the intent of public functions with a short comment.

This file intentionally overlaps with CLAUDE.md so the Token Analyzer can demonstrate
content-overlap (redundancy) detection.
